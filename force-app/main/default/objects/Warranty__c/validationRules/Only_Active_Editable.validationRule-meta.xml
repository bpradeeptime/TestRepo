<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Only_Active_Editable</fullName>
    <active>true</active>
    <description>Prevents editing of warranties that are not active. Only warranties whose prior Status is Active may be modified. New records are always allowed.</description>
    <errorConditionFormula>AND(
    NOT(ISNEW()),
    NOT(ISPICKVAL(PRIORVALUE(Status__c), &quot;Active&quot;))
)</errorConditionFormula>
    <errorMessage>This warranty is not active and cannot be edited. Only active warranties may be modified.</errorMessage>
</ValidationRule>
